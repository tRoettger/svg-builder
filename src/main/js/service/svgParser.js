const { LOAD_SVG, SVG_DOCUMENT_ELEMENT } = require("./common/serviceCenter");

class SvgParser {

    constructor() {
        this.parse = this.parse.bind(this);
    }
    
    parse(svgAsString) {

    }

}

const svgParser = new SvgParser();